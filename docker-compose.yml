version: '3.8'
services:
  dev:
    build: .
    command: sh -c "tail -f /dev/null"
    working_dir: /src
    volumes:
      - ./:/src:delegated
      - /src/node_modules
      - /src/dist
      - /var/run/docker.sock:/var/run/docker.sock
    ports:
      - 4400:4200
